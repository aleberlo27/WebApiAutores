namespace WebApiAutores.Servicios
{
    public interface IServicio
    {
        /*
        Guid ObrenerScoped();
        Guid ObrenerSingleton();
        Guid ObrenerTransient();
        void RealizarTarea();

        public class ServicioA : IServicio
        {
            private readonly ILogger<ServicioA> logger;
            private readonly ServicioTransient servicioTransient;
            private readonly ServicioScoped servicioScoped;
            private readonly ServicioSingleton servicioSingleton;

            public ServicioA(ILogger<ServicioA> logger, ServicioTransient servicioTransient, 
                ServicioScoped servicioScoped, ServicioSingleton servicioSingleton)
            {
                this.logger = logger;
                this.servicioTransient = servicioTransient;
                this.servicioScoped = servicioScoped;
                this.servicioSingleton = servicioSingleton;
            }

            public Guid ObrenerTransient() { return servicioTransient.Guid; }
            public Guid ObrenerScoped() { return servicioScoped.Guid; }
            public Guid ObrenerSingleton() { return servicioSingleton.Guid; }

            public void RealizarTarea()
            {
                
            }
        }
        public class ServicioB : IServicio
        {
            public Guid ObrenerScoped()
            {
                throw new NotImplementedException();
            }

            public Guid ObrenerSingleton()
            {
                throw new NotImplementedException();
            }

            public Guid ObrenerTransient()
            {
                throw new NotImplementedException();
            }

            public void RealizarTarea()
            {
               
            }
        }
        */
    }

    /*
    public class ServicioTransient
    {
        public Guid Guid = Guid.NewGuid();
    }

    public class ServicioScoped
    {
        public Guid Guid = Guid.NewGuid();
    }

    public class ServicioSingleton
    {
        public Guid Guid = Guid.NewGuid();
    }
    */
}

